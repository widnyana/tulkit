name: supply-chain

on:
  pull_request:
    paths:
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
  push:
    branches: [main]
  schedule:
    # Weekly re-scan catches advisories disclosed after a PR merged.
    - cron: "0 6 * * 1"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
        with:
          version: 11

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: pnpm

      # Scanner: fail on known-vulnerable dependencies (tighten to moderate if desired).
      - name: pnpm audit
        run: pnpm audit --audit-level=high

      # Flag dependencies that request install/build scripts — the primary malware
      # vector. pnpm blocks these by default; approve intentional ones via
      # `pnpm approve-builds` (recorded in pnpm-workspace.yaml allowBuilds).
      - name: Flag install scripts
        run: |
          pnpm install --frozen-lockfile 2>&1 | tee install.log
          if grep -qi "ignored build scripts" install.log; then
            echo "::warning::New/updated dependencies request build scripts — review before approving."
          fi
