minimumReleaseAge: 10080

allowBuilds:
  "@parcel/watcher": true
  "@swc/core": true
  sharp: true

# Force patched picomatch (ReDoS GHSA-c2c7-rcm5-vvqj) under next-intl's
# @parcel/watcher > micromatch chain. pnpm 11 reads overrides here, not package.json.
overrides:
  picomatch: ^2.3.2
  nanoid: ^3.3.18
